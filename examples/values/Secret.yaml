
Secret:
  # https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.28/#secret-v1-core
  # bool
  immutable: true
  # "Opaque" "kubernetes.io/service-account-token" "kubernetes.io/dockercfg" "kubernetes.io/dockerconfigjson" "kubernetes.io/basic-auth" "kubernetes.io/ssh-auth" "kubernetes.io/tls" "bootstrap.kubernetes.io/token"
  # string
  type: xx
  # slice, map
  data:
    a: 123
  # map 类型，Key 会作为新的文件名
  # string, slice, map
  dataFiles:
    - ce: config/envs.yaml
  # slice, map
  stringData:
    b: 123
  # map 类型，Key 会作为新的文件名
  # string, slice, map
  stringDataFiles:
    stringDataxx: config/envs.yaml
stringData:
- cc: xxx
stringDataFiles:
- config/envs.yaml
